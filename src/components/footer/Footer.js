/* Currently just padding, but could potentially be a place for contact us, more info, newletter sign up, etc*/
import './Footer.css';

const Footer = () => {
    return (
        <div className = "footer">
        </div>
    );
}

export default Footer;